func main() -> void
{
    let a:integer;
    let b:integer;

    let c:integer;

    // Read a and b
    read(a);
    read(b);

    c = f1(a,b);

    write(c);
}

func f1(a:integer, b:integer) -> integer
{
    return (a+b);
}
